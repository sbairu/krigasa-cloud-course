title: Krigasa-cloud
description: Internal Labs
template:
  repo: krigasa-cloud-template
  name: krigasa-cloud
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
  - title: New step
    actions:
      - type: createIssue
        title: A new issue
        body: a-response-file.md
      - type: createLabel
